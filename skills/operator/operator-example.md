# Operator Example

## Input

Esegui la validazione di tutte le skill presenti in `skills/` e riportami il risultato.

## Skill Used

operator

## Output

Procedure run:

- Validazione delle skill locali.

Inputs used:

- Directory: `skills/`
- Validatore: `scripts/validate_skills.py`

Commands or actions executed:

- Elencate le sottocartelle in `skills/`.
- Eseguito `scripts/validate_skills.py` sulle skill locali.

Outputs produced:

- `scout`: valid
- `planner`: valid
- `builder`: valid
- `critic-qa`: valid
- `librarian`: valid
- `operator`: valid
- `guardian`: valid

Anomalies:

- Nessuna anomalia.

Completion status:

- Procedura completata.

## Why This Matters

Questo output è migliore di una risposta generica perché descrive procedura, input, comandi, risultati e anomalie invece di limitarsi a dire che "sembra tutto ok".
